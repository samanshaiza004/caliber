[package]
name = "caliber"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Exact Git source dependency tooling for Caliber consumers"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
caliber-ffi = { path = "../caliber-ffi", version = "0.0.0" }
